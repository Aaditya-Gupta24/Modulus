"""Streamlit UI for MechOpt.

IMPLEMENT after the core library passes its tests. Wire inputs (load, length,
load case, FoS target, cost/weight priority) to mechopt.optimizer and show the
recommendation, the candidate table, and the tradeoff plots.

Run locally:  streamlit run app.py
"""

import streamlit as st  # noqa: F401

# TODO: build the UI once mechopt.optimizer is implemented and green.
